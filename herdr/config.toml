# herdr configuration
# https://herdr.dev/docs/configuration/
# Run `herdr --default-config` to inspect the complete schema.
# Apply changes with `herdr server reload-config`.

[keys]
# Keep the default prefix (ctrl+b) bindings while adding direct ctrl+alt+X
# chords so commands can also run without a prefix.
# Reasons for choosing ctrl+alt:
# - Ghostty consumes cmd chords at the terminal layer before they reach herdr.
# - Bare ctrl+letter conflicts with zsh widgets (ctrl+g/f/], etc.) and readline.
# - herdr also recommends explicit modified chords for reliable direct bindings.
# herdr is not intended to run inside cmux, so cmux shortcut conflicts are not considered.

# --- Pane ---
focus_pane_left  = ["prefix+h", "ctrl+alt+h"]
focus_pane_down  = ["prefix+j", "ctrl+alt+j"]
focus_pane_up    = ["prefix+k", "ctrl+alt+k"]
focus_pane_right = ["prefix+l", "ctrl+alt+l"]
split_vertical   = ["prefix+v", "ctrl+alt+v"]
split_horizontal = ["prefix+minus", "ctrl+alt+s"]
close_pane       = ["prefix+x", "ctrl+alt+x"]
zoom             = ["prefix+z", "ctrl+alt+z"]
resize_mode      = ["prefix+r", "ctrl+alt+r"]

# --- Tab ---
new_tab      = ["prefix+c", "ctrl+alt+t"]
previous_tab = ["prefix+p", "ctrl+alt+left"]
next_tab     = ["prefix+n", "ctrl+alt+right"]
switch_tab   = ["prefix+1..9", "ctrl+alt+1..9"]

# --- Workspace ---
workspace_picker   = ["prefix+w", "ctrl+alt+w"]
previous_workspace = "ctrl+alt+up"
next_workspace     = "ctrl+alt+down"

# --- Misc ---
goto            = ["prefix+g", "ctrl+alt+g"]
edit_scrollback = ["prefix+e", "ctrl+alt+e"]
toggle_sidebar  = ["prefix+b", "ctrl+alt+b"]

[ui]
agent_panel_sort = "priority"
