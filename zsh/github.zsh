#!/bin/zsh
# GitHub-related aliases and functions
# opg is now a standalone script at bin/opg (callable from GUI apps, cron, etc.)

# ghu (GitHub Utility) aliases
alias get='ghu get'
alias init='ghu init'
